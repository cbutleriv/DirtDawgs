---
/**
 * Eyebrow — the "———————— SECTION LABEL" pattern used above every section headline.
 * dark:  true when sitting on a navy background (swaps divider color)
 */
interface Props {
  num?: string; /* kept for compatibility — no longer rendered */
  dark?: boolean;
  class?: string;
}

const { dark = false, class: className } = Astro.props;
---

<div class:list={['eyebrow', { dark }, className]}>
  <span class="line" aria-hidden="true"></span>
  <span class="label"><slot /></span>
</div>

<style>
  .eyebrow {
    display: flex;
    align-items: center;
    gap: 18px;
  }

  .line {
    flex: 1;
    height: 1px;
    background: var(--color-divider);
    min-width: 24px;
  }

  .dark .line {
    background: var(--color-divider-d);
  }

  .label {
    font-family: var(--font-label);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--color-ink-3);
    flex-shrink: 0;
  }

  .dark .label {
    color: rgba(250, 250, 247, 0.6);
  }
</style>
